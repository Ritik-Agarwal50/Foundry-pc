
To install Library we use this commands
`forge install smartcontractkit/chainlink-brownie-contracts@0.6.1 --no-commit`
You can specify version using `@` look above


We can Make Fake address to send Money in our contract 
command - 
`uint256 Name = makeAddr("Name")`
`vm.prank(Name)` - this will tell the test that this user gonna be next who gonna send money or eth

`chisel` 
-> it will help u to wrtiee and execute the code in the terminal itself ,isn't amazing?